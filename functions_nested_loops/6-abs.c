#include "main.h"
/**
 * _abs - The absolute value of integer
 * Description : give back the absolute value
 *@n:The value will take form
 * Return:If its a postive return the same else the negative way
 */
int _abs(int n)
{
	if (n > 0)
		return (n * -1);

	return (0);
}
