#include "main.h"
/**
  * mul - multiplies two ints
  * @a: one integer
  * @b: second integer
  * Return: product of a and b
  */

int mul(int a, int b)
{
	int multi;

	multi = (a * b);

	return (multi);
}
